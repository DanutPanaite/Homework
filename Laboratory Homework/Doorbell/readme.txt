A program that uses the passive buzzer from the Arduino kit to detect if a person knocked. 
If true, after five seconds since knocking the active buzzer will play a doorbell like sound 
which can be stopped by simply pressing the button at any time.
